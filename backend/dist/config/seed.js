/**
 * seed.ts — MongoDB seed (no-op stub)
 *
 * MongoDB seeding has been removed. PostgreSQL is seeded via seedPostgres.ts.
 * This stub is retained for any import references that haven't been cleaned up yet.
 */
export async function seedDatabase() {
    // No-op: PostgreSQL seeding is handled by seedPostgres.ts
}
